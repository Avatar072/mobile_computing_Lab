#: import C kivy.utils.get_hex_from_color

<ImageUploadLayout>:
    orientation: 'vertical'
    
    Button:
        text: 'Upload Image'
        on_release: root.select_image()
        size_hint_y: None
        height: '48dp'
        pos_hint: {'x': 0, 'y': 1}

    Image:
        id: image_preview
        source: ''